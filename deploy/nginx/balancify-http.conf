server {
    listen 80;
    listen [::]:80;
    server_name hei.heifam.com api.heifam.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 'balancify bootstrap';
        add_header Content-Type text/plain;
    }
}
